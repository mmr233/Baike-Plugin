# 迁移说明

原始单文件版本仍保留在工作区根目录的 `百科查询.js`，本次没有覆盖它。

新的插件结构目录为：

```text
Baike-Plugin/
```

## 建议迁移方式

1. 将 `Baike-Plugin` 整个目录放入 `Yunzai/plugins/`
2. 安装依赖：`pnpm install --filter=baike-plugin`
3. 重启 Yunzai
4. 打开锅巴面板检查配置是否正确
5. 使用 `#百科帮助` 验证帮助页

## 命令对照

- 搜索：`搜索 xxx`
- 自然语言搜索：`xxx是谁` / `xxx是什么`
- 内容总结：`总结 + 引用消息`
- 群聊总结：群里直接发送 `总结`
- 更新：`#百科更新` / `#百科强制更新`
